<template>
  <!-- A code block. overflow-x-auto so a long line scrolls inside the block
     instead of widening the whole conversation. -->
  <pre class="my-3 overflow-x-auto rounded-lg border border-muted bg-elevated p-3 text-xs leading-relaxed"><code><slot /></code></pre>
</template>
