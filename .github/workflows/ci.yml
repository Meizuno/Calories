name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  # The checks live in tests.yml so deploy.yml can run exactly the same set
  # before publishing an image.
  tests:
    uses: ./.github/workflows/tests.yml
